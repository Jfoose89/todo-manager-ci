# Todo Manager CI Project

A simple Todo Manager implemented in JavaScript with full Jest test coverage and a GitHub Actions CI pipeline.

## Scripts

- `npm test` — run tests
- `npm run test:coverage` — run tests with coverage

## Features

- Add, update, remove todos
- Toggle completed state
- Clear completed todos
- 80% minimum coverage threshold
